const styles = theme => ({
    colorDark: {
      backgroundColor: theme.palette.secondary.dark,
  
    },
    colorMain: {
      color: theme.palette.primary.main,
  
    },
    colorLight: {
      color: theme.palette.primary.main
    },
    menuListCursor: {
      cursor: 'pointer'
    }
  
  });

  export default styles;